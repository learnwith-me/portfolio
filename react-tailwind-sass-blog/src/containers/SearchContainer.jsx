import React from 'react'
import TopSearch from '../components/search'

const SearchContainer = ({searchResults}) => {
  return (
    <TopSearch searchResults={searchResults} />
  )
}

export default SearchContainer